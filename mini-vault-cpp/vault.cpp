#include <iostream>
#include <vector> 
#include <string>
#include "vault.h"
using namespace std;

    void setField(){

    }



   void addcredential(vector<credential> creds){
    credential c;
    bool requiresUsername; // checks whether website uses a username vs simply email adress
    cout << "What website is this for?" << endl;
    }
    void addCredential(std::vector<credential>& creds){}
    void saveCredential(const std::vector<credential>& creds){}
    void loadCredential(std::vector<credential>& creds, const std::string& filename){}
    void viewCredential(const std::vector<credential>& creds){}
    void setField(std::string entry){}